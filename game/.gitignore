# Godot-generated artifacts — regenerated by the CI `godot --headless --import`
# step and intentionally NOT tracked (they are absent on main). Ignoring them
# keeps the working tree clean after a local import/export run.
.godot/
*.import

# Local export output.
build/
